// burger

$(document).ready(function () {
   $(".menu__icon").click(function () {
       if ($(this).hasClass("open")) {
           $(".menu__body").removeClass("open");
           $(this).removeClass("open");
           $("body").removeClass("_lock");
       } else {
           $(".menu__body").addClass("open");
           $(this).addClass("open");
           $("body").addClass("_lock");
       }
   });
   $(".menu__link, .header__button.register").click(function () {
       $(".menu__body").removeClass("open");
           $('.menu__icon').removeClass("open");
           $("body").removeClass("_lock");    
   });
});

// header

$(document).ready(function () {
   let lastScrollTop = 0;
   $(window).scroll(function () {
      const scrollTop = $(window).scrollTop(); 
      if (scrollTop >= 100) {      
         if (scrollTop > lastScrollTop + 5) {
            $('.header').addClass('scrolled');
         } 
         else if (scrollTop < lastScrollTop) {
            $('.header').removeClass('scrolled');
         }
      } else { 
         $('.header').removeClass('scrolled '); 
      }
      lastScrollTop = scrollTop; 
      });
});


// slider   

if ($('.main-faq__slider').length) {
   $('.main-faq__slider').slick({
      infinite: false,
      slidesToShow: 3,
      slidesToScroll: 1,
      dots: false,
      prevArrow: $('.main-faq__slider-left'),
      nextArrow: $('.main-faq__slider-right'),
      responsive: [
        
         {
            breakpoint: 767,
            settings: {
               slidesToShow: 2,
            }
         },
         {
            breakpoint: 550,
            settings: {
               slidesToShow: 1,
               arrows: false,
            }
         }
      ]
   });
}

if ($('.reviews-main__website-images').length) {
   $('.reviews-main__website-images').slick({
      infinite: true,
      slidesToShow: 3.25,
      dots: false,
      arrows: false,
   });
}
if ($('.main__website-left').length) {
   $('.main__website-left').slick({
      infinite: true,
      slidesToShow: 1,
      dots: false,
      arrows: false,
       responsive: [
         {
            breakpoint: 992,
            settings: {
               slidesToShow: 3,
            }
         },
        
      ]
   });
}
